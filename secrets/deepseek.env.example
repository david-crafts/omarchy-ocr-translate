# DeepSeek API key template (do not commit a real key).
# Copy to ~/.config/omarchy/secrets/deepseek.env and fill in your own value.
# Sourced only by ~/.local/bin/omarchy-translate; do not export into the desktop session.
# Replace the empty value with the key from https://platform.deepseek.com (placeholder only — not a real key).
DEEPSEEK_API_KEY=
